# CLAUDE.md — /app

Contexto completo en `AGENTS.md` (mismo directorio) — escrito para ser
leído por cualquier agente (Claude Code u otro). Léelo antes de tocar
código aquí; no está duplicado en este archivo a propósito, para que no se
desalinee con el tiempo.

Recordatorio corto: este subproyecto sigue el protocolo `addv-web-app` de
la raíz del repo (`../CLAUDE.md`) — analizar → revisar impacto → criticar/
mejorar → propuesta visual antes/después → confirmar → implementar →
probar → asegurar. El `index.html` de la raíz del repo es intocable desde
aquí (sitio ya en producción).
